# 작업로그 · 2026-07-04 — 결제(S7) + 주문완료(S8)

> 주문 결제 흐름 추가. 개발용 모의(mock) 결제 기본, 실제 PortOne 연동 준비 + 가이드. (PR #23)

## 1. 워크플로우
1. [x] 서버 결제 도메인(검증기 mock/real) + POST /api/orders/{id}/pay + 주문 상태 결제대기/결제완료
2. [x] 서버 빌드검증(ASCII,H2) + MySQL 실측(mock 결제)
3. [x] 앱 결제수단 선택(S7) + 주문완료(S8) + 주문서 연동 + analyze/test
4. [x] 커밋/PR(#23) → dev 머지 + 결제 연동 가이드/작업로그

## 2. 리포트 (PR #23)

### 서버 (05_API §5)
- `payment`: `PaymentVerifier` — Mock(기본 `app.payment.mock=true`) / Real(PortOne, false) + `PaymentResult`.
- `POST /api/orders/{id}/pay` : '결제대기' 주문 검증 후 '결제완료'.
  - 결제수단 검증(CARD/KAKAOPAY/NAVERPAY/TOSSPAY/SAMSUNGPAY), **서버금액=실결제금액 재검증(조작 방지)**, 본인만, 중복결제 409.
- `OrderEntity`: `paymentMethod`/`paidAt` + `markPaid()`. 주문 생성 상태 '결제대기'.

### 앱
- `OrderApi`: `createOrder`가 orderId 포함 결과 반환 + `payOrder()`.
- 결제 화면(S7): 결제수단 선택 + 모의 결제. 주문완료(S8): 주문번호 + 주문내역/홈.
- 주문서 '결제하기' → 주문 생성 → 결제 → 주문완료.

## 3. 검증
- 서버 gradlew build(H2) 성공(결제완료/중복결제 409/잘못된 수단 400 테스트 포함).
- MySQL 실측: 주문 생성(결제대기) → 결제(KAKAOPAY, mock) → 결제완료, 재결제 409.
- 앱 flutter analyze 무결점 + test 6건.

## 4. 실제 연동
- 가이드: `가이드/결제_연동.md`. 요약: PortOne 가입·PG 채널·V2 API Secret →
  `app.payment.mock=false` + `PORTONE_API_SECRET` 환경변수 → 앱 결제창(paymentId) 연결.

## 5. 다음 단계
- 결제 성공 시 적립 지급·쿠폰 차감(§10), 관리자 주문 상태 관리(A4), 결제 실패/취소 처리.
