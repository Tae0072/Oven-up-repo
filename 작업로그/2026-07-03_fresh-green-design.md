# 작업로그 · 2026-07-03 — 프레시 그린 디자인 + 로고 스플래시

> 디자인 시안 중 "프레시 그린(건강·수제)" 컨셉을 앱에 전면 적용. 로딩 화면에 오븐업 손그림 로고 애니메이션. (PR #22)

## 1. 배경
- 시안 비교(HTML 목업 2차례) 후 사용자가 "프레시 그린" 선택.
- 로딩 화면에 로고 이미지(`로고, 이미지/KakaoTalk_20260703_142755084.png`, 손그림 라인아트)를 애니메이션으로 사용 요청.

## 2. 워크플로우
1. [x] 로고 이미지 에셋 추가(assets/images/logo.png) + pubspec 등록
2. [x] 프레시 그린 색 시스템(AppColors) + main 테마 전환 + 스플래시 로고 애니메이션
3. [x] 홈·마이페이지·메뉴 BEST 뱃지 그린/옐로우 톤 적용 + 로그인 화면 로고
4. [x] analyze/test 검증 + 커밋/PR(#22) → dev 머지 + 작업로그

## 3. 리포트 (PR #22)
- **색 시스템** `theme/app_colors.dart`: primary `#17A968`(그린), primaryDark `#0C7D4C`, accent `#FFC93C`(옐로우), bg `#F4FAF5`, text `#16241C`.
- **테마**: `main.dart` `ColorScheme.fromSeed(그린)` + `scaffoldBackgroundColor`.
- **스플래시(S0)**: 그린 그라데이션 배경 위 흰 카드에 로고. 단계 애니메이션 —
  ① 카드 페이드+스케일(easeOutBack) → ② 로고 페이드 → ③ 문구 슬라이드업. 약 2.1s 후 로그인/홈 이동.
  (무한 반복 애니메이션은 테스트 pumpAndSettle 무한대기 위험이 있어 일회성으로 구성.)
- **화면**: 홈 배너·바로가기 아이콘, 마이페이지 관리자 표기, 메뉴 카드·상세 BEST 뱃지(옐로우+진초록 글자). 로그인 화면 상단 로고 노출.
- 로고 로드 실패 대비 `errorBuilder`(빵 아이콘) 지정.

## 4. 검증
- flutter analyze 무결점. flutter test 6건 통과(스플래시 문구·에셋 로드 확인 포함).

## 5. 남은 일
- 메뉴 실제 사진 적용(현재 이모지 placeholder).
- 앱 아이콘·네이티브 스플래시 리소스(안드로이드/웹) 교체 — 지금은 앱 내부 첫 화면만 로고 적용.
